{
  "name": "pwtest",
  "version": "1.0.0",
  "description": "test playwright+mocha",
  "main": "index.js",
  "scripts": {
    "test": "mocha --require ts-node/register ./specs/**/*.spec.ts --no-timeout",
    "test:reporter": "mocha --timeout 50000 ./specs/**/*.spec.ts --reporter allure-mocha"
  },
  "keywords": [
    "pw",
    "mocha"
  ],
  "author": "Mikhail Pankov",
  "license": "ISC",
  "dependencies": {
    "@playwright/test": "^1.23.4",
    "@types/chai": "^4.3.1",
    "@types/mocha": "^9.1.1",
    "chai": "^4.3.6",
    "dotenv": "^16.0.1",
    "faker": "^5.5.3",
    "mocha": "^10.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^4.7.4"
  }
}

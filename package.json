{
  "name": "pwtest",
  "version": "1.0.0",
  "description": "test playwright+mocha",
  "main": "index.js",
  "scripts": {
    "test": "mocha ./specs/**/*.spec.js --no-timeout",
    "test:reporter": "mocha --timeout 50000 ./specs/**/*.spec.js --reporter allure-mocha"
  },
  "keywords": [
    "pw",
    "mocha"
  ],
  "author": "Mikhail Pankov",
  "license": "ISC",
  "devDependencies": {
    "playwright": "^1.19.2"
  },
  "dependencies": {
    "@playwright/test": "^1.20.0",
    "allure-mocha": "^2.0.0-beta.15",
    "chai": "^4.3.6",
    "dotenv": "^16.0.0",
    "faker": "^5.5.3",
    "mocha": "^9.2.2"
  }
}
